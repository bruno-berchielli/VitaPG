en:
  backup_runs:
    detail_component:
      started: "Started"
      duration: "Duration"
      size: "Size"
      trigger: "Trigger"
      restore: "Restore with"
      restore_tip: "Run this on a machine with PostgreSQL client tools after downloading the file. Custom and directory dumps use pg_restore; plain SQL pipes into psql. It only writes to the database YOU point it at."
      triggers:
        scheduled: "Scheduled"
        manual: "Manual"
pt-BR:
  backup_runs:
    detail_component:
      started: "Início"
      duration: "Duração"
      size: "Tamanho"
      trigger: "Disparo"
      restore: "Restaurar com"
      restore_tip: "Execute isto numa máquina com as ferramentas de cliente do PostgreSQL após baixar o arquivo. Dumps custom e directory usam pg_restore; SQL puro vai para o psql. Só escreve no banco que VOCÊ indicar."
      triggers:
        scheduled: "Agendado"
        manual: "Manual"
es:
  backup_runs:
    detail_component:
      started: "Inicio"
      duration: "Duración"
      size: "Tamaño"
      trigger: "Disparo"
      restore: "Restaurar con"
      restore_tip: "Ejecuta esto en una máquina con las herramientas de cliente de PostgreSQL tras descargar el archivo. Los dumps custom y directory usan pg_restore; el SQL plano va a psql. Solo escribe en la base que TÚ indiques."
      triggers:
        scheduled: "Programado"
        manual: "Manual"
